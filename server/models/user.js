'use strict';

const async = require('async');
let randomstring = require('randomstring');

module.exports = function(User) {
  const self = User;
};

module.exports = function(User) {
  let a = 5;
  return a;
}